CREATE TABLE public.jga_layer_group
(
    layer_group_id integer NOT NULL DEFAULT nextval('jga_layer_group_layer_group_id_seq'::regclass),
    updated_at timestamp without time zone NOT NULL,
    CONSTRAINT jga_layer_group_pkey PRIMARY KEY (layer_group_id)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.jga_layer_group
    OWNER to postgres;
COMMENT ON TABLE public.jga_layer_group
    IS 'GeOAPレイヤグループ更新日時管理テーブル';

COMMENT ON COLUMN public.jga_layer_group.layer_group_id
    IS 'レイヤグループID';

COMMENT ON COLUMN public.jga_layer_group.updated_at
    IS '最終更新日時';

    704
     5852
     11068

     ①ロックがかかっているか確認
     ②ロックがかかっているものを消す
     ③postgresを再起動